unit n2_prot_camb_estado;

interface

uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  un1_refe_camb_estado, Db, RxMemDS, PictureContainer, DBTables, Placemnt,
  AdvPanel, dxExEdtr, dxCntner, SCLPropiedadesForm, DBActns, ActnList,
  ImgList, dxDBTLCl, dxGrClms, dxTL, dxDBCtrl, dxDBGrid,
  ufra_list_referencias, dxEdLib, dxDBELib, RxLookup, PCPLookUpComboEdit,
  Boxes, PCPFrame, dxPageControl, ExtCtrls, dxEditor, StdCtrls, SoCtrls,
  SCLDBLabel, TB2Item, TBX, TB2Dock, TB2Toolbar;

type
  Tfn1_refe_camb_estado1 = class(Tfn1_refe_camb_estado)
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  fn1_refe_camb_estado1: Tfn1_refe_camb_estado1;

implementation

{$R *.DFM}

end.
